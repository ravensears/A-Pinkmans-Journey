class Treasure {
  constructor(x, y, width, height, message) {
    this.x = x,
    this.y = y,
    this.width = width,
    this.height = height,
    this.message = message
  }
}

export default Treasure